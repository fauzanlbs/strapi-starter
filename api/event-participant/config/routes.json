{
  "routes": [
    {
      "method": "GET",
      "path": "/event-participants",
      "handler": "event-participant.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/event-participants/count",
      "handler": "event-participant.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/event-participants/:id",
      "handler": "event-participant.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/event-participants",
      "handler": "event-participant.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/event-participants/:id",
      "handler": "event-participant.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/event-participants/:id",
      "handler": "event-participant.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
